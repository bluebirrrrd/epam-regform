export class Person {
    constructor (
        firstName: String,
        lastName: String,
        email: String,
        password: String,
        dateOfBirth: Date,
        postalIndex: String,
        country: String
    ) { };
}
